#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    uint64_t a, i, b;

    cin >> a >> i;

    b = (a * (i - 1)) + 1;
    cout << b << endl;
}